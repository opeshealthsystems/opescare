import { Text } from 'react-native';
import { Screen } from '../../components/ui/Screen';

/** Placeholder — built out in Phase 1 parallel fan-out per the design spec. */
export default function RecordsScreen() {
  return (
    <Screen className="items-center justify-center">
      <Text className="text-base text-navy-secondary">Records — coming in Phase 1.</Text>
    </Screen>
  );
}
