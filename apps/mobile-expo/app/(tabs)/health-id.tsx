import { Text } from 'react-native';
import { Screen } from '../../components/ui/Screen';

/** Placeholder — full Health ID card + QR built out in Phase 1 parallel fan-out. */
export default function HealthIdScreen() {
  return (
    <Screen className="items-center justify-center">
      <Text className="text-base text-navy-secondary">Health ID — coming in Phase 1.</Text>
    </Screen>
  );
}
